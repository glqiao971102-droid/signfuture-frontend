import AdminInstallations from "@/components/admin/AdminInstallations";

export const metadata = { title: "Installations — Admin" };

export default function Page() {
  return <AdminInstallations />;
}
