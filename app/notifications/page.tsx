import SitePage from "@/components/SitePage";

export const metadata = { title: "Notifications — Sign Studio" };

export default function Page() {
  return <SitePage title="Notifications" blurb="You have no new notifications." />;
}
