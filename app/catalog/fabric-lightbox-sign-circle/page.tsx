import ProductFrame from "@/components/ProductFrame";

export const metadata = { title: "Fabric Lightbox Sign (Circle) — Sign Calculators" };

export default function Page() {
  return (
    <ProductFrame
      src="/apps/fabric-lightbox-sign-circle/index.html"
      title="Fabric Lightbox Sign (Circle)"
    />
  );
}