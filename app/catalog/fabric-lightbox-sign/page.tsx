import ProductFrame from "@/components/ProductFrame";

export const metadata = { title: "Fabric Lightbox Sign — Sign Calculators" };

export default function Page() {
  return (
    <ProductFrame
      src="/apps/fabric-lightbox-sign/index.html"
      title="Fabric Lightbox Sign"
    />
  );
}