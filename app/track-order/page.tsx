import SitePage from "@/components/SitePage";

export const metadata = { title: "Track Order — Sign Studio" };

export default function Page() {
  return <SitePage title="Track Order" blurb="Enter an order number to track its status — coming soon." />;
}
